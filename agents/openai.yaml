interface:
  display_name: "Unslop"
  short_description: "Cut AI tells from writing"
  default_prompt: "Use $unslop to improve this draft. Use writing.lint only for public-facing or reusable work after the draft is ready."
policy:
  allow_implicit_invocation: true
